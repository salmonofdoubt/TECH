#ifndef UTILS_H
#define UTILS_H 1

/* Append an element to a path
 * Returns a new string with path_prefix + "/" + path
 */
extern char * path_append(const char *path_prefix, const char *path);

#endif

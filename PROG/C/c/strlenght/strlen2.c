#include <stdio.h>
#include <string.h>

/*
#define CHUNKSIZE 1024

void print_array(int *data, int n) {
  int i = 0;
  for (i = 0; i < n; i++) {
    printf("%d: %d\n", i, data[i]);
  }
}

void print_average(int *data, int n) {
  int i = 0;
  double sum = 0;
  for (i = 0; i < n; i++) {
    sum += data[i];
  }
  printf("Average: %f\n", sum / n);
}
*/
int main(int argc, char *argv[]) {
  
  int s[100];
  printf("The length of %s is %d chars.\n", s, strlen(s));
  return 0;
}

  exit(EXIT_SUCCESS);


